# =============================================================================
# RH Gestao (CertfAI) - Environment Variables Example
# =============================================================================
# Copy this file to .env and fill in the values
# IMPORTANT: Never commit .env file with real credentials to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get these values from your Supabase project dashboard:
# https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# Supabase Project URL
VITE_SUPABASE_URL=https://uuorwhhvjxafrqdyrrzt.supabase.co

# Supabase Anonymous/Public Key (safe to expose in frontend)
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# -----------------------------------------------------------------------------
# n8n Webhook Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# These are used for document processing via n8n automation

# n8n Webhook URL for document processing
VITE_N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/your-webhook-id

# n8n Webhook Authentication Token (RECOMMENDED for security)
# Set this to require authentication on your webhook
VITE_N8N_WEBHOOK_AUTH_TOKEN=your-secure-token-here

# -----------------------------------------------------------------------------
# OpenAI Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# Used for semantic comparison features

# OpenAI API Key
VITE_OPENAI_API_KEY=sk-your-openai-api-key-here

# -----------------------------------------------------------------------------
# Application Settings (OPTIONAL)
# -----------------------------------------------------------------------------

# Enable debug mode (shows additional logs in development)
# VITE_DEBUG_MODE=true

# =============================================================================
# SECURITY NOTES:
# =============================================================================
# 1. NEVER commit real credentials to version control
# 2. Rotate all keys if they were ever exposed
# 3. Use different credentials for development and production
# 4. The Supabase anon key is designed to be public, but still rotate if exposed
# 5. VITE_N8N_WEBHOOK_AUTH_TOKEN should be a long, random string
# =============================================================================
